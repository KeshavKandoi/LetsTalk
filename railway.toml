[build]
builder = "nixpacks"
buildCommand = "corepack enable && corepack prepare pnpm@9 --activate && cd Frontend && pnpm install && pnpm run build"

[deploy]
startCommand = "cd Frontend && node dist/server/server.js"
restartPolicyType = "ON_FAILURE"
