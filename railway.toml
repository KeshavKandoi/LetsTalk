[build]
builder = "nixpacks"
buildCommand = "cd Backend && npm ci && cd ../Frontend && corepack enable && corepack prepare pnpm@9 --activate && pnpm install && pnpm run build"
[deploy]
startCommand = "cd Frontend && node start-server.mjs"
restartPolicyType = "ON_FAILURE"
