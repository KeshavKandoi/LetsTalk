[build]
builder = "nixpacks"
buildCommand = "npm install -g pnpm@9 && export PATH=$PATH:$(npm root -g)/.bin && cd Frontend && pnpm install && pnpm run build"

[deploy]
startCommand = "cd Frontend && node dist/server/server.js"
restartPolicyType = "ON_FAILURE"
